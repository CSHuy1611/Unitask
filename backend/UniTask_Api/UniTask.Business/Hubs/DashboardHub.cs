using Microsoft.AspNetCore.SignalR;

namespace UniTask.Business.Hubs
{
    public class DashboardHub : Hub
    {
        // Add specific methods if client needs to send data directly to hub.
        // For now, it will act primarily as a broadcast channel from server to clients.
    }
}
